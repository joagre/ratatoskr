fn foo(f, g, x) {
    if f(x =Int= 1) {
        g(x)
    } else {
        20
    }
}

/*
fn foo(f is (Bool) -> Bool, g is (Int) -> Int, x is Int)
  is ((Bool) -> Bool, (Int) -> Int, Int) -> Int {
...
*/
