fn foo(f, g, x) {
    //?x := (?y := 2),
    if f(x =Int= 1) {
        g(x)
    } else {
        20
    }
}










// t0 -> ({(Bool -> Bool), (Int -> Int), Int} -> Int)

/*
fn foo(f is ([Bool] -> Bool),
       g is ([Int] -> Int),
       x is Int) is Int {
*/