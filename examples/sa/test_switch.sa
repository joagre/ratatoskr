export fn main(_) -> Bool {
    zonk((1, true))
}

fn zonk(a) {
    switch a {
        case (1, ?c) || (3, ?c) when c =Bool= true =>
            c
        case (2, 1) =>
            true
        default =>
            true
    }
}
