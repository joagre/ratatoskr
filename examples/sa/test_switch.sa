export fn main(_) {
    zonk((42, 1))
}

fn zonk(a is (Int, Int)) -> (Int, (Int, Int)) {
    switch a {
        case (4711, 4711) as ?a =>
            (0, a)
        case (?a, 1), (?a, 2) as ?b is (Int, Int) when a == 42 =>
            (a, (a, b))
        default =>
            (0, (0, 0))
    }
}
