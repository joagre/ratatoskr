record Person<$t> {
    public name: (Int, Int)
}

export fn zip() {
    Person(name: ?w) := Person(name: "Joe"),
    w
//    foo(42)
//    ?p1 := Person(name: "Joe"),
//    42 := p1.name
}

fn foo(x: Int) {
//    42
   fn (x) { x + 1 }
//    "Joe" := zip()
}