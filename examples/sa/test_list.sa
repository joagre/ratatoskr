//export fn main(_) {
//    84 := foo(42)
//}

//export fn main(_) {
//    ?l is [Int] := foo(42)
//}


fn foo(x) {
    ?l is [Int] := [1, 2, 3],
    ?l2 := l @ [4, 5]
    //1 := l2[0]
    //5 := l2[$ - 1],
    //?l3 := [[fn (x) { x + 1 }, fn (x) { x + 2 }],
    //        [fn (x) { x * 1 }, fn (x) { x * 2 }]],
    //l3[0][1](x)
}
